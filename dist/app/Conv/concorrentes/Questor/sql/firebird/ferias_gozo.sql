SELECT CODIGOEMPRESA,
       CODIGOFUNCCONTR,
       DATAINICIALFERIAS,
       CAST(DATAFINALFERIAS - DIASABONO AS DATE) AS DATAFINALFERIAS,
       CAST(DIASABONO AS INTEGER) AS DIASABONO,
       DATAABONO,
       CAST(DATAABONO + DIASABONO -1 AS DATE) AS DATAFINALABONO,
       DATAPGTO,
       PAGAADTO13,
       DATAINICIAL,
       TIPORECIBO
  FROM RECIBOFERIAS
 ORDER BY CODIGOEMPRESA,
          CODIGOFUNCCONTR,
          DATAINICIALFERIAS
