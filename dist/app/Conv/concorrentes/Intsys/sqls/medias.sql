SELECT EMPRE_FICI,
       FUNCI_FICI,
       COMPE_FICI,
       EVENT_FICI,
       REFER_FICI,
       VALOR_FICI
FROM TABFPFICI
LEFT JOIN TABFPEVEN
	ON TABFPFICI.EVENT_FICI = TABFPEVEN.CODIG_EVEN
WHERE COMPE_FICI > (EXTRACT(YEAR FROM current_date) - 3) || '01'
ORDER BY 1, 3, 2, 4