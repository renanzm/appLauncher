SELECT CODIGO,
CNPJ, 
RAZAOSOCIAL, 
LOGRADOURO, 
NUMERO, 
BAIRRO, 
CEP, 
(SELECT max(MUN.NOME) FROM MUN WHERE MUN.CODIGO = AIN.MUN_CODIGO AND MUN.UFD_SIGLA = AIN.UFD_SIGLA) AS NOME_MUNICIPIO,
UFD_SIGLA,
ENDCOMPLEMENTO
FROM AIN
ORDER BY 3