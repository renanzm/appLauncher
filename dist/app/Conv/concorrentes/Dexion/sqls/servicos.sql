SELECT pl.*,
       e.DATA_ATIVIDADE,
       e.CNAE,
       e.INSS_FPAS,
       e.OUTRAS_ENTIDADES_CODIGO,
       e.GPS_CODIGO_PAGAMENTO
  FROM P_LOTACOES pl
  LEFT JOIN EMPRESAS e
    ON e.EMPRESA = pl.EMPRESA
 WHERE pl.TIPO_LOTACAO_SEFIP != 1
 ORDER BY 1, 2;