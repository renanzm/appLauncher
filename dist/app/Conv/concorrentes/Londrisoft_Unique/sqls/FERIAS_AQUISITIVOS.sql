SELECT CD_EMPRESA,
       CD_FUNCIONARIO,
       DT_INI_AQUISICAO,
       DT_FIM_AQUISICAO,
       SUM(DIAS_FALTAS) AS DIAS_FALTAS
  FROM RHFERIAS
 WHERE DT_INI_AQUISICAO IS NOT NULL
 GROUP BY 1, 2, 3, 4
 ORDER BY 1, 2, 3;