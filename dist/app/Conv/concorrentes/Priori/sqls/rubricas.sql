SELECT EVENTOS.EVENTO,
       EVENTOS.NOME,
       EVENTOS.TRATAMENTO,
       COALESCE(EVENTOS.FERIAS_FORMULA, 0) AS FERIAS_FORMULA,
       COALESCE(EVENTOS.D3_FORMULA, 0) AS D3_FORMULA
  FROM EVENTOS
 WHERE EVENTOS.TIPO = '+'
 ORDER BY 1;