SELECT REGI.EMPRE_REGI,
	   REGI.TIPOS_REGI,
       REGI.TPANX_EMPR
FROM dpcomp.tblregi AS REGI
WHERE REGI.DATAS_REGI = (
	SELECT MAX(SUB.DATAS_REGI)
	FROM dpcomp.tblregi AS SUB
    WHERE SUB.EMPRE_REGI = REGI.EMPRE_REGI
)
ORDER BY 1