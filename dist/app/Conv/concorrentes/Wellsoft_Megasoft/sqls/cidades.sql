SELECT CIDADES.ID_CIDADES,
       CIDADES.DESCRICAO,
       ESTADOS.SIGLA AS ESTADO,
       CIDADES.CODIGO_IBGE
FROM CIDADES
INNER JOIN ESTADOS
	ON CIDADES.FK_ESTADO = ESTADOS.ID_ESTADOS
ORDER BY 3, 2