SELECT COD_EMP,
       COD_FUN,
       DT_AQUIS_INI_FER,
       DT_AQUIS_FIN_FER,
       SUM(QUANT_FALTAS_FER) AS DIAS_FALTAS
  FROM TB_LANFERIAS_FER
 WHERE DT_AQUIS_INI_FER IS NOT NULL
 GROUP BY 1, 2, 3, 4
 ORDER BY 1, 2, 3;