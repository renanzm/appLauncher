SELECT CD_EMPRESA,
       CD_EMPREGADO,
       DT_INICIO,
       DT_TERMINO,
       DATEDIFF(day, DT_INICIO, DT_TERMINO) AS DIAS,
       ST_PRORROGADO
  FROM CONTROLECONT
 WHERE TP_CONTRATO = 'E'
 ORDER BY CD_EMPRESA,
          CD_EMPREGADO,
          DT_INICIO;